# Install CUDA-enabled PyTorch wheels (compatible with CUDA 13.x drivers)
--extra-index-url https://download.pytorch.org/whl/cu128

# Stem separation
torch
torchaudio
demucs>=4.0.1

# Audio I/O and processing
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0
librosa>=0.10.1
scipy>=1.11.0
pyrubberband>=0.3.0

# UI
PySide6>=6.6.0

# Packaging (optional — only needed for building .exe)
pyinstaller>=6.0.0